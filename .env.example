VITE_API_BASE_URL=

# Externally-hosted hero demo video (CDN / object storage URL). Kept out of the
# repo and Docker image because of its size. Falls back to /simulator-example.MP4
# (a local public/ copy) when unset.
VITE_HERO_VIDEO_URL=
